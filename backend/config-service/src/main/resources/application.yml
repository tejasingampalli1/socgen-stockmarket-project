spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/tejasingampalli1/stockmarket-configserver
          default-label: main
          clone-on-start: true